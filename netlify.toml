# 모든 함수 관련 설정은 이 헤더 아래에 위치합니다.
[functions]
  # 함수 실행 시간을 26초로 넉넉하게 설정합니다.
  timeout = 26

  # [중요] sharp 라이브러리가 필요로 하는 모든 파일을
  # 배포 시에 반드시 포함하도록 지정합니다.
  # 이것이 없으면 sharp가 서버에서 작동하지 않습니다.
  included_files = ["node_modules/sharp/**"]
